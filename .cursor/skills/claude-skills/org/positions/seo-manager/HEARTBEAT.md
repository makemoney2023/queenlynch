# HEARTBEAT — seo-manager

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `seo-manager`, `report_to: cmo`, `llm_tier: fast-ops`. Pin model.
2. **Packet** — Re-read goal, inputs, must_read, outputs, write_lease, constraints.
3. **Playbook** — Open `## Phase playbooks` → Phase **2**, **14**, or **16** in `SKILL.md`.
4. **Phase 2 note** — When spawned by **head-of-research**, follow Phase 2 playbook; handoff to HoR merge path.
5. **Leases** — Write **only** leased paths.
6. **Spawn** — **None.** IC seat — `ask_manager` for eng SEO fixes (sitemap, robots).
7. **Tools** — Use GSC, firecrawl, PageSpeed per TOOL-REGISTRY; set `tool_status` if unavailable.
8. **Handoff** — Write `HANDOFFS/<phase>-seo-manager.md` with model audit.
9. **Exit** — Do **not** mark phase ✅.
