# HEARTBEAT — content-strategist

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `content-strategist`, `report_to: cmo`, `llm_tier: strong-general`. Pin model.
2. **Packet** — Re-read goal, inputs, must_read, outputs, write_lease, constraints.
3. **Playbook** — Open `## Phase playbooks` → Phase **6**, **13**, **14**, or **17** in `SKILL.md`.
4. **Leases** — Write **only** leased paths (GTM sections, copy foundation slices, blog, social).
5. **Spawn** — **None.** IC seat — `ask_manager` for brand social stills (Phase 17).
6. **Production** — Phase 17: set `production_status` for social assets; lifecycle owns email HTML in parallel.
7. **Handoff** — Write `HANDOFFS/<phase>-content-strategist.md` with model audit (+ production fields on 17).
8. **Exit** — Do **not** mark phase ✅.
