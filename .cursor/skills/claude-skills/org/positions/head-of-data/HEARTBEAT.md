# HEARTBEAT — head-of-data

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `head-of-data`, `report_to: ceo-strategist`, `llm_tier: strong-general`. Pin model.
2. **Packet** — Re-read goal, inputs, must_read, outputs, write_lease, constraints.
3. **Mode** — Phase **20** = manager → Phase 20 playbook. Phase **22** = on-demand peer only (orchestrator dispatch) → peer playbook; do not spawn.
4. **Playbook** — Open `## Phase playbooks` → active mode in `SKILL.md`.
5. **Leases** — Write only leased paths; analytics-engineer gets non-colliding subset of `20-analytics.md`.
6. **Delegates** — Phase 20: spawn **only** `analytics-engineer`. Phase 22: spawn **nobody**.
7. **Collaborate** — When tagged with `cmo` / `paid` on Phase 22, align KPI narrative in peer brief — do not spawn them.
8. **Handoffs** — Phase 20: await IC; merge; write `HANDOFFS/20-manager-head-of-data.md`. Phase 22: write `HANDOFFS/22-peer-head-of-data.md` for CEO merge.
9. **Escalate / exit** — Do **not** mark phase ✅. Leave next action for orchestrator / CEO.
