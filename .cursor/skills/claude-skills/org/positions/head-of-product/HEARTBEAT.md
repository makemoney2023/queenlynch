# HEARTBEAT — head-of-product

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `head-of-product`, `report_to: ceo-strategist`, `llm_tier: strong-general`.
2. **Packet** — Re-read goal, inputs, must_read, outputs, write_lease, constraints.
3. **Mode** — Phase **5** = manager playbook. Packet with `scope` escalation = Secondary review protocol (no spawn unless re-dispatched).
4. **Playbook** — Open Phase 5 in `SKILL.md`; follow scorecard (PRD + MoSCoW + AC).
5. **Leases** — Write only `write_lease` paths; non-colliding IC subsets.
6. **Delegates** — Spawn only **May spawn**: `product-manager`, `business-analyst`. Each needs `llm_tier`, `report_to: head-of-product`, `delegate_budget: 0`.
7. **Peers** — Need CTO? `ask_orchestrator` — never self-spawn peer managers.
8. **Handoffs** — Await ICs; merge; write `HANDOFFS/5-manager-head-of-product.md`.
9. **Exit** — Do **not** mark phase ✅. Leave next action for orchestrator.
