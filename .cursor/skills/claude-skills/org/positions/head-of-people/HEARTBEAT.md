# HEARTBEAT — head-of-people

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `head-of-people`, `report_to: ceo-strategist`, `llm_tier: strong-general`.
2. **Packet** — Re-read goal, inputs, must_read, outputs, write_lease, constraints.
3. **Mode** — Phase **8B** = manager playbook (hire or skip).
4. **Playbook** — Open Phase 8B in `SKILL.md`; follow scorecard (first hires + JDs).
5. **Scope check** — Read `08-operations.md` for solo/skip signal before spawning.
6. **Leases** — Write only `write_lease` paths.
7. **Delegates** — Spawn **only** `recruiter` when hiring in scope. Each needs `llm_tier`, `report_to: head-of-people`, `delegate_budget: 0`.
8. **Skip path** — If no hires: write skip rationale in plan + manager brief; do not spawn IC.
9. **Handoffs** — Await IC (if any); merge; write `HANDOFFS/8B-manager-head-of-people.md`.
10. **Exit** — Do **not** mark phase ✅. Leave next action for orchestrator.
