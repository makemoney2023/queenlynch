# HEARTBEAT — head-of-sales-cs

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `head-of-sales-cs`, `report_to: ceo-strategist`, `llm_tier: strong-general`.
2. **Packet** — Re-read goal, inputs, must_read, outputs, write_lease, constraints.
3. **Mode** — Phase **7** = manager playbook only.
4. **Playbook** — Open Phase 7 in `SKILL.md`; follow scorecard (close + retain).
5. **Leases** — Write only `write_lease` paths; non-colliding IC subsets on `07-sales-playbook.md`.
6. **Delegates** — Spawn only **May spawn**: `sales-enablement-lead`, `outbound-lead`, `customer-success-manager` (parallel). Each needs `llm_tier`, `report_to: head-of-sales-cs`, `delegate_budget: 0`.
7. **Peers** — Need CMO/GTM alignment? `ask_orchestrator` — never self-spawn peer managers.
8. **Handoffs** — Await ICs; merge close + respond + retain; write `HANDOFFS/7-manager-head-of-sales-cs.md`.
9. **Exit** — Do **not** mark phase ✅. Leave next action for orchestrator.
