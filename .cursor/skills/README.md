# Queen Lynch Pharmacy — project skills

Copied from the Wilk & Wilk / Strong Foam rebuild toolkit (`Desktop/Projects/wilkandwilk/.cursor/skills`).

| Skill | Role |
|-------|------|
| `scroll-craft` | Premium scroll-world grammar, fingerprint gate, GSAP engine |
| `gemini-omni` + `ai-video-gen` | Animate stills → short loops (optional; needs GEMINI_API_KEY) |
| `img2threejs` | Optional photo → Three.js craft moments |
| `seo-geo-aeo` + `notfair-seo/*` + `schema-markup-generator` | AEO/SEO/GEO + JSON-LD |
| `blog-writing` + `content-planner` | Answer-first blog / calendar |
| `brand` / `design` / `design-system` / `ui-styling` / `ui-ux-pro-max` | Visual system |
| `natural-human-voice` | Copy tone |
| `shadcn-ui` | Component adapter |
| `obsidian-secrets` | Resolve API keys from vault / `.env.local` |

Build pattern to mirror: **Wilk & Wilk** (Next.js App Router + GSAP ScrollTrigger photographic scroll-world + media sync from `assets/` → `public/media/` + SEO routes). Strong Foam adds Three/R3F + estimate survey — use only if it fits pharmacy IA.
