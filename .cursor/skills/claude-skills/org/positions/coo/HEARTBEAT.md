# HEARTBEAT — coo

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `coo`, `report_to: ceo-strategist`, `llm_tier: frontier-reasoning`. Pin model; do not inherit parent.
2. **Packet** — Re-read goal, inputs, must_read, outputs, write_lease, constraints.
3. **Mode** — Phase **8** = manager playbook. Phase **0** = peer brief only. Packet with `legal` escalation = Secondary review protocol (no IC spawn unless re-dispatched as manager).
4. **Playbook** — Open `## Phase playbooks` → active phase in `SKILL.md`. Follow scorecard.
5. **Leases** — Only write paths in `write_lease`. Give each IC a non-colliding subset.
6. **Delegates** — Spawn **only** **May spawn** for the phase: Phase 8 → `ops-manager` + `legal-counsel` (parallel). Each IC needs `llm_tier`, `report_to: coo`, `delegate_budget: 0`.
7. **Phase 0** — Do not spawn; write `HANDOFFS/0-manager-coo.md` only.
8. **Handoffs** — Await IC handoffs; merge ops + risk; write manager brief (MANAGER-BRIEF-TEMPLATE).
9. **Legal secondary** — Comment on csuite review when tagged; do not self-approve.
10. **Exit** — Do **not** mark phase ✅. Leave next action for orchestrator.
