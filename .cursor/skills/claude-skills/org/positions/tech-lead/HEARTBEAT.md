# HEARTBEAT — tech-lead

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `tech-lead`, `report_to: cto`, `llm_tier: coding-agent`, `generation_profile: none`. Pin model.
2. **Packet** — Re-read goal, inputs, must_read, outputs, `write_lease`, constraints.
3. **Playbook** — Open `## Phase craft playbooks` → Phase 9 in `SKILL.md`.
4. **Tools** — Load `skills/integrations/context7-docs/` before guessing library APIs. Load `skills/integrations/playwright-browser/` when smoking live MVP routes. Record `tool_status` if MCP unavailable.
5. **Leases** — Write only `09-build-log.md` + `apps/<venture>/` (and routes named in lease).
6. **TDD** — Failing test → implement → refactor; cite test evidence in build log.
7. **PRD scope** — Must/Should from `05-prd.md`; drift → `ask_manager` (`scope→HoP`).
8. **Production** — Runnable MVP under `apps/<venture>/` or honest `production_status: skipped`. Smoke critical paths via Playwright when claiming complete.
9. **Handoff** — Write `HANDOFFS/9-tech-lead.md` with production fields + model audit + tools used.
10. **Peers** — **Never spawn** verifier or other ICs. Do **not** mark phase ✅.
