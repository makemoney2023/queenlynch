# HEARTBEAT — company-orchestrator

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm orchestrator / main-session dispatcher; `llm_tier: frontier-reasoning` → `grok-4.5`.
2. **Venture** — Resolve `projects/registry.json` `active`; only write that venture’s `business-idea/` + MEMORY.
3. **Read** — ORG-REGISTRY, MODEL-REGISTRY, TOOL-REGISTRY, COLLABORATION, ESCALATION, this `SKILL.md`.
4. **Queue** — Prefer oldest `DISPATCH/queue/*.yaml` → claim → spawn **manager only** with packet `llm_tier`.
5. **Fan-out** — Never spawn ICs directly (exception: act as / spawn `ceo-strategist` on CEO-owned phases). Manager May-spawn comes from position skills.
6. **Leases** — When reviewing manager plans / packets, enforce non-colliding IC leases (`COLLABORATION.md` § Parallel IC leases).
7. **Shippable** — After manager brief on 4B/9/9B/11/12/14/15/17/18/19/21: verifier pass before C-suite approve.
8. **Phase 22** — Weekly one-bullet standup **or** deep peers via `HANDOFFS/22-peer-<slug>.md` for HoD / CMO / paid-media-manager; CEO merges.
9. **Gate** — Phase ✅ only after csuite `verdict: approve` (Phase 0 may skip-review).
10. **Exit** — Durable next action (tracker row, DISPATCH state, or operator ask).
11. **Stay dispatcher after shippable** — Once Phase 9 (or any shippable) has a Layer B app, bugs, PDF alignment, and design passes are new packets to `cto` or `creative-director`. This session does not implement them.
12. **Classification skips** — Honor CLASSIFICATION-SKIPS.md. Do not spawn skipped ICs or queue skipped phases without an operator waiver on the register.
13. **Agency beats** — brief → concept → craft → edit → produce → verify → client review → revise/ship. Do not mark ready for C-suite until inbox `artifact_path` is the client file, quality scorecard passes, and Open register items that block this seat are not marked done.
