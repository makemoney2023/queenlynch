# HEARTBEAT — cfo

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm position `cfo`, `report_to: ceo-strategist`, and `llm_tier: frontier-reasoning`. Pin model; do not inherit parent.
2. **Packet** — Re-read goal, inputs, must_read, outputs, write_lease, constraints. Do not invent packs.
3. **Mode** — Phase **4** / **4B** = manager → matching playbook. Phase **0** = peer brief only. Packet with `spend` escalation = Secondary review protocol (no IC spawn unless re-dispatched as manager).
4. **Playbook** — Open `## Phase playbooks` → active phase in `SKILL.md`. Follow scorecard.
5. **Leases** — Only write paths in `write_lease`. Give each IC a non-colliding subset.
6. **Delegates** — Spawn **only** **May spawn** for the phase: Phase 4 → `fpa-analyst` + `product-marketing-manager`; Phase 4B → `fundraising-lead`. Each IC needs `llm_tier`, `report_to: cfo`, `delegate_budget: 0`.
7. **Phase 4B** — Enforce Office reject gate (pptx + xlsx or skip); await verifier before C-suite.
8. **Phase 0** — Do not spawn; write `HANDOFFS/0-manager-cfo.md` only.
9. **Handoffs** — Await IC handoffs; merge; write manager brief (MANAGER-BRIEF-TEMPLATE).
10. **Escalate / exit** — Use ESCALATION tags when needed. Do **not** mark the phase ✅. Leave next action for orchestrator.
