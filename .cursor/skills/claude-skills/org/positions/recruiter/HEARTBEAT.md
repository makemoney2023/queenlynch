# HEARTBEAT — recruiter (IC)

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `recruiter`, `report_to: head-of-people`, `llm_tier: fast-ops`.
2. **Packet** — Re-read goal, inputs, must_read, outputs, `write_lease`, constraints.
3. **Scope** — Check `08-operations.md` for 8B skip; if hiring out of scope, document skip — do not invent JDs.
4. **Playbook** — Open Phase 8B hiring craft in `SKILL.md`.
5. **Leases** — Write only hiring sections of `08b-people-plan.md`.
6. **Spawn** — **Never spawn.** Comp bands / RACI? `ask_manager` only.
7. **Handoff** — Write `HANDOFFS/8B-recruiter.md` with model audit fields.
8. **Exit** — Do **not** mark phase ✅. Do **not** write manager brief.
