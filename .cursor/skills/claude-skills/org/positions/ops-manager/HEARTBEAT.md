# HEARTBEAT — ops-manager (IC)

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `ops-manager`, `report_to: coo`, `llm_tier: fast-ops`.
2. **Packet** — Re-read goal, inputs, must_read, outputs, `write_lease`, constraints.
3. **Playbook** — Open Phase 8 ops runbook craft in `SKILL.md` (ops slice — not legal).
4. **Leases** — Write only ops sections of `08-operations.md`; do not author legal/risk checklist.
5. **Cross-ref** — Sales SLAs live in `07-sales-playbook.md` — cross-ref, do not duplicate scripts.
6. **Spawn** — **Never spawn.** Legal/build peers? `ask_manager` only.
7. **Handoff** — Write `HANDOFFS/8-ops-manager.md` with model audit fields.
8. **Exit** — Do **not** mark phase ✅. Do **not** write manager brief.
