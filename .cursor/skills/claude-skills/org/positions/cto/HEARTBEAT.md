# HEARTBEAT — cto

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `cto`, `report_to: ceo-strategist`, `llm_tier: coding-agent`. Pin model; do not inherit parent.
2. **Packet** — Re-read goal, inputs, must_read, outputs, write_lease, constraints. Load `production-artifacts` on Phase 9/9B.
3. **Mode** — Phase **9** / **9B** = manager → matching playbook. Packet with `scope` escalation = Secondary review protocol (no IC spawn unless re-dispatched as manager).
4. **Playbook** — Open `## Phase playbooks` → active phase in `SKILL.md`. Follow scorecard (build log + Layer B + verifier).
5. **Tools (Phase 9)** — Expect tech-lead to use `skills/integrations/context7-docs/` before library APIs and `skills/integrations/playwright-browser/` for MVP smoke. Use Context7 yourself when reviewing stack choices; Playwright secondary for oversight smoke if needed.
6. **Leases** — Only write paths in `write_lease`. Give each IC a non-colliding subset; tech-lead must lease `apps/<venture>/` when MVP claimed.
7. **Delegates** — Spawn **only** **May spawn** for the phase: Phase 9 → `tech-lead` then `verifier`; Phase 9B → `hardware-engineer` then `verifier`. Craft IC first; verifier **after** manager brief. Verifier: `report_to: cto`, write handoff only.
8. **Production** — Reject missing `production_status`; reject `complete` without real app/CAD; require honest skip reason.
9. **Verifier** — Await `HANDOFFS/<phase>-verifier.md` with `verdict: pass` before C-suite (or skip confirmed).
10. **Handoffs** — Await ICs; merge; write `HANDOFFS/<phase>-manager-cto.md` with Production check.
11. **Collaborate** — HoP via orchestrator on scope; design-system SSOT at repo root — do not duplicate in app.
12. **Escalate / exit** — scope→HoP tags per ESCALATION.md. Do **not** mark phase ✅. Leave next action for orchestrator.
