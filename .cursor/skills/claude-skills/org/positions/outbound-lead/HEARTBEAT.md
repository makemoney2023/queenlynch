# HEARTBEAT — outbound-lead (IC)

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `outbound-lead`, `report_to: head-of-sales-cs`, `llm_tier: strong-general`.
2. **Packet** — Re-read goal, inputs, must_read, outputs, `write_lease`, constraints.
3. **Playbook** — Open Phase 7 Respond + outbound craft in `SKILL.md` (Part II).
4. **GTM check** — Confirm inbound-only vs outbound-in-scope; skip outbound craft honestly when GTM says so.
5. **Leases** — Write only leased paths (`07-sales-playbook.md` Part II; `17-channels/emails/` when leased).
6. **Spawn** — **Never spawn.** Need lifecycle/copy overlap? `ask_manager` only.
7. **Handoff** — Write `HANDOFFS/7-outbound-lead.md` with model audit fields.
8. **Exit** — Do **not** mark phase ✅. Do **not** write manager brief.
