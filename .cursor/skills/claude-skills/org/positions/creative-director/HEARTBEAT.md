# HEARTBEAT — creative-director

Run top-to-bottom every spawn/wake.

1. **Identity** — Confirm `creative-director`, `report_to: ceo-strategist`, `llm_tier: creative-language`. Pin model.
2. **Packet** — Re-read goal, inputs, must_read, outputs, write_lease, constraints, `budget_usd` (Phase 15).
3. **Mode** — Manager phases **11, 12, 15** → matching playbook. Not owner on 14/17/19 unless orchestrator reassigns.
4. **Playbook** — Open `## Phase playbooks` → active phase. All three owned phases are **shippable**.
5. **Tools (Phase 11 / 12)** — When Figma URLs or DS sync are in scope: load `skills/integrations/figma/`; auth via MCP before design ops; never invent tokens; else `tool_status: unavailable`.
6. **Leases** — Write only leased paths; non-colliding IC subsets. DS SSOT: `design-system/<venture>/` at repo root.
7. **Delegates** — Spawn **only** **May spawn** for this phase (11 → brand-designer; 12 → web-designer ± brand-designer; 15 → video-producer). Each IC: `llm_tier`, `report_to: creative-director`, `delegate_budget: 0`, `generation_profile` when rendering.
8. **Production** — On 11/12/15: reject missing `production_status`; require stills / design-system / OpenMontage finals or skip; review `photoreal_qa`; await verifier via orchestrator/CTO.
9. **Handoffs** — Await ICs; merge; write `HANDOFFS/<phase>-manager-creative-director.md` with Production check.
10. **Collaborate** — CMO may own brand/video on 14/17/19 — consult RACI; use `ask_orchestrator` when track ownership unclear. Escalate `brand→CD`, `spend→cfo`. Do **not** mark phase ✅.
11. **Exit** — Durable next action for orchestrator.
